<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.concerto.dao.PdfItemDao">

    <select id="getAll" resultType="PdfItem">
        select * from pdf ;
    </select>

    <insert id="insertPdfItem" parameterType="PdfItem" useGeneratedKeys="true" keyProperty="pdfId" >
        insert into pdf(pdfDescription, pdfName, pdfUpdateTime,type,subject)
            value (#{pdfDescription},#{pdfName},#{pdfUpdateTime},#{type},#{subject})
    </insert>

    <insert id="deletePdfItem" parameterType="long" >
        delete from pdf where pdfId=#{pdfId};
    </insert>

    <select id="selectByName" resultType="PdfItem">
        select * from pdf where pdfName like CONCAT('%',#{keyword},'%') or pdf.pdfDescription like CONCAT('%',#{keyword},'%');
    </select>

</mapper>