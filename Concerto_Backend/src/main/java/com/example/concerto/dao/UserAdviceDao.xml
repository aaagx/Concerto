<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.concerto.dao.UserAdviceDao">

    <insert id="insertAdvice" parameterType="UserAdvice"  >
        insert into user_advice( user_id, advice_content) values (#{userId},#{adviceContent});
    </insert>

</mapper>